# Решение проблемы: Таблицы не заполняются данными

## Описание проблемы

При нажатии кнопки "Пересоздать таблицы" таблицы создавались, но **не заполнялись тестовыми данными**.

## Причина проблемы

В файле `app.go`, метод `RecreateTables()` вызывал только:

```go
func (a *App) RecreateTables() RecreateTablesResult {
    err := database.CreateTables()  // ← Создаёт таблицы
    // ... обработка ошибки
    return RecreateTablesResult{
        Success: true,
        Message: "Таблицы успешно пересозданы",
    }
}
```

**Но не вызывал** функцию `database.SeedData()`, которая загружает тестовые данные!

## Почему это происходило

1. В `internal/database/migrations.go` существует функция `CreateTables()` которая:

   - Удаляет старые таблицы
   - Создаёт новые таблицы на основе моделей
   - Но **не заполняет их данными**

2. В `internal/database/seed.go` существует функция `SeedData()` которая:

   - Читает SQL-скрипт из файла `seed.data.sql`
   - Выполняет INSERT команды для заполнения таблиц
   - **Но не вызывалась после создания таблиц**

3. Функции были разделены, но `RecreateTables()` не связывал их воедино!

## Решение

**Было:**

```go
func (a *App) RecreateTables() RecreateTablesResult {
    err := database.CreateTables()
    if err != nil {
        return RecreateTablesResult{
            Success: false,
            Message: "Не удалось пересоздать таблицы",
            Error:   err.Error(),
        }
    }
    return RecreateTablesResult{
        Success: true,
        Message: "Таблицы успешно пересозданы",
    }
}
```

**Стало:**

```go
func (a *App) RecreateTables() RecreateTablesResult {
    // Шаг 1: Пересоздаём таблицы
    err := database.CreateTables()
    if err != nil {
        return RecreateTablesResult{
            Success: false,
            Message: "Не удалось пересоздать таблицы",
            Error:   err.Error(),
        }
    }

    // Шаг 2: Загружаем тестовые данные
    err = database.SeedData()
    if err != nil {
        return RecreateTablesResult{
            Success: false,
            Message: "Таблицы пересозданы, но не удалось загрузить тестовые данные",
            Error:   err.Error(),
        }
    }

    return RecreateTablesResult{
        Success: true,
        Message: "Таблицы успешно пересозданы и заполнены данными",
    }
}
```

## Что теперь происходит

1. Frontend отправляет запрос на `RecreateTables()`
2. Backend:
   - Удаляет старые таблицы
   - Создаёт новые таблицы (пустые)
   - Читает `seed.data.sql` файл
   - Выполняет INSERT команды для всех таблиц:
     - **products** - 12 записей
     - **production_batches** - 20 записей
     - **inventory** - 12 записей
     - **sales** - 25 записей
   - Обновляет sequences для PostgreSQL (для AUTO_INCREMENT полей)
3. Frontend получает успешный ответ
4. Таблицы теперь содержат все тестовые данные!

## Как это работает

### Файл seed.data.sql содержит:

```sql
BEGIN;

-- таблица продуктов(12 записей)
INSERT INTO products (product_id, name, flavor, volume_ml, price, caffeine_level, ingredients) VALUES
(1, 'Энергетик Boost', 'Лимон-мята', 250, 99.90, 'high', 'вода, сахар, кофеин, таурин, ...'),
...
(12, 'Витаминный напиток VitaZest', 'Гранат', 330, 119.00, 'medium', '...');

-- таблица производственные партии(20 записей)
INSERT INTO production_batches (batch_id, product_id, production_date, quantity_produced) VALUES
...

-- таблица склад(12 записей)
INSERT INTO inventory (inventory_id, product_id, quantity_in_stock, last_updated) VALUES
...

-- таблица продаж(25 записей)
INSERT INTO sales (sale_id, product_id, sale_date, quantity_sold, total_price) VALUES
...

COMMIT;
```

### Функция SeedData() делает:

1. **Поиск файла:**

   - Ищет `seed.data.sql` в несколько местах:
     - Текущая директория
     - Директория исполняемого файла
     - Папка пакета `internal/database/`

2. **Чтение файла:**

   - Читает SQL-скрипт целиком

3. **Выполнение в транзакции:**

   - Выполняет весь SQL в одной транзакции (либо всё выполнится, либо ничего)
   - Для PostgreSQL обновляет sequences для AUTO_INCREMENT

4. **Обработка ошибок:**
   - Если что-то пошло не так - вся транзакция откатывается (ROLLBACK)
   - Возвращает подробное сообщение об ошибке

## Проверка на практике

После исправления:

1. Откройте приложение в браузере
2. Перейдите на главную страницу
3. Нажмите кнопку "Пересоздать таблицы"
4. Вы должны увидеть уведомление: "Таблицы успешно пересозданы и заполнены данными"
5. Перейдите на страницу "Структура БД" чтобы увидеть все таблицы с данными

## Преимущества такого подхода

✅ **Модульность** - CreateTables() и SeedData() отделены друг от друга  
✅ **Переиспользуемость** - SeedData() можно вызывать отдельно если нужно  
✅ **Надежность** - Обе операции в транзакциях (либо всё, либо ничего)  
✅ **Информативность** - Сообщение об ошибке указывает на конкретный шаг  
✅ **Логирование** - Все действия логируются для отладки

## Похожие проблемы в будущем

Если вы хотите добавить новые данные или операции при создании таблиц, просто добавьте ещё один шаг:

```go
func (a *App) RecreateTables() RecreateTablesResult {
    // 1. Создаём таблицы
    err := database.CreateTables()
    if err != nil { /* ... */ }

    // 2. Загружаем данные
    err = database.SeedData()
    if err != nil { /* ... */ }

    // 3. Выполняем дополнительную инициализацию (например)
    err = database.InitializeIndexes()
    if err != nil { /* ... */ }

    return RecreateTablesResult{
        Success: true,
        Message: "Таблицы успешно пересозданы, заполнены данными и индексированы",
    }
}
```

## Файл который был изменён

- **`app.go`** - Метод `RecreateTables()` (строка 521)
  - Добавлен вызов `database.SeedData()` после `database.CreateTables()`
  - Добавлена обработка ошибок для загрузки данных

## Связанные файлы (не изменялись, но важны)

- `internal/database/migrations.go` - Функция `CreateTables()` (создание таблиц)
- `internal/database/seed.go` - Функция `SeedData()` (загрузка данных)
- `internal/database/seed.data.sql` - SQL скрипт с тестовыми данными
- `frontend/src/pages/home-page/HomePage.tsx` - Кнопка на frontend

## Заключение

Проблема была простой: две функции на backend которые нужно было вызвать последовательно, но вторая не вызывалась. Исправление заняло 1 строку кода (плюс 1 строка для обработки ошибок), но зато теперь всё работает как положено! 🎉
