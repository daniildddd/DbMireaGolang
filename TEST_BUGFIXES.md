# Инструкция по тестированию исправлений

## ✅ Исправление 1: Бесконечная загрузка на "Схема БД"

### Что было исправлено

- React Query больше не отправляет запросы с пустым tableName
- Компонент DatabaseStructurePage установит currentTable ДО создания хука
- useTableSchema имеет параметр `enabled` для предотвращения пустых запросов

### Как протестировать

#### Тест А: Быстрая загрузка страницы

```
1. Откройте приложение в браузере (http://localhost:34115)
2. Нажмите на вкладку "Структура БД" (или перейдите /database-structure)
3. Измерьте время загрузки (должно быть < 1 сек)
4. Убедитесь что видны:
   ✓ Список всех таблиц
   ✓ Поля каждой таблицы (name, type, constraints)
   ✓ Нет Loading спиннера (или виден очень недолго)
```

#### Тест B: Проверка отсутствия ошибок

```
1. Откройте DevTools браузера (F12)
2. Перейдите на вкладку Console
3. Убедитесь что НЕТУ ошибок типа:
   ❌ "Table  not found"
   ❌ "unsupported data type"
   ❌ "ERR: failed to fetch schema"
4. Должны быть только нормальные сообщения (или ничего)
```

#### Тест C: Проверка Network запросов

```
1. Откройте DevTools браузера (F12)
2. Перейдите на вкладку Network
3. Откройте страницу "Структура БД"
4. Посмотрите на Wails API запросы:
   ✓ GetTableNamesFromModels() → 1 запрос
   ✓ GetTableSchema() → 4 запроса (по одному для каждой таблицы)
   ✓ Итого: ~5 запросов (МАКСИМУМ)
5. Убедитесь что запросов КОНЕЧНОЕ количество (не растёт)
```

#### Тест D: Переключение между таблицами

```
1. На странице "Структура БД" смотрите полученные таблицы
2. Каждая таблица должна быстро загрузиться
3. При переключении между таблицами - нет Loading
4. Все данные корректны (column names, types, constraints)
```

---

## ✅ Исправление 2: Остающиеся ограничения при возврате

### Что было исправлено

- useEffect cleanup функция очищает `filters` при размонтировании
- Очищаются также `queryResults` и `activeModal`
- Таблица (currentTable) НЕ очищается (это нормально)

### Как протестировать

#### Тест A: WHERE фильтр

```
1. Откройте страницу "Фильтрация"
2. Выберите таблицу (например "products")
3. Нажмите кнопку "WHERE"
4. Добавьте условие:
   - Field: "price"
   - Operator: ">"
   - Value: "100"
5. Нажмите "Применить"
6. Убедитесь что в "Итоговый SQL:" появился WHERE условие
7. ВАЖНО: Нажмите кнопку "Удалить" чтобы удалить это условие
8. Убедитесь что условие исчезло из SQL
9. Перейдите на другую страницу (например "Структура БД")
10. Вернитесь на "Фильтрация"
11. ✓ WHERE условие ИСЧЕЗЛО (не должно быть)
12. ✓ Таблица "products" всё ещё выбрана (это OK)
```

#### Тест B: GROUP BY + AGGREGATE

```
1. На странице "Фильтрация" выберите таблицу "products"
2. Нажмите кнопку "GROUP BY"
3. Добавьте "name" в GROUP BY
4. Нажмите "Применить"
5. Нажмите кнопку "AGGREGATE"
6. Добавьте COUNT(id) с alias "total"
7. Нажмите "Применить"
8. В SQL должны быть: GROUP BY name, COUNT(id) as total
9. Теперь нажмите НАЗАД на каждый элемент и удалите их:
   - Откройте GROUP BY → удалите "name"
   - Откройте AGGREGATE → удалите COUNT
10. Убедитесь что SQL очищен
11. Перейдите на "Структура БД"
12. Вернитесь на "Фильтрация"
13. ✓ Нет GROUP BY, нет AGGREGATE (очищено)
```

#### Тест C: ORDER BY

```
1. На странице "Фильтрация" выберите "sales"
2. Нажмите "ORDER BY"
3. Добавьте сортировку:
   - Field: "sale_date"
   - Direction: "DESC"
4. Нажмите "Применить"
5. В SQL должно быть: ORDER BY sale_date DESC
6. Откройте ORDER BY снова
7. Нажмите "Удалить" для удаления этой сортировки
8. SQL должен очиститься
9. Перейдите на "Фильтрация" (на другом пути)
   - например нажмите Back в браузере
10. Вернитесь на "Фильтрация"
11. ✓ ORDER BY исчезнул (не вернулся)
```

#### Тест D: CASE WHEN

```
1. На странице "Фильтрация" выберите таблицу
2. Нажмите "CASE"
3. Добавьте WHEN-THEN пару:
   - Field: "price"
   - Operator: ">"
   - Value: "100"
   - Result: "expensive"
4. Нажмите "Добавить пару"
5. Введите ELSE значение: "cheap"
6. Нажмите "Применить"
7. В SQL должна быть: CASE WHEN price > 100 THEN 'expensive' ELSE 'cheap'
8. Откройте CASE снова и удалите пару
9. SQL должен очиститься
10. Перейдите в другой раздел приложения
11. Вернитесь на "Фильтрация"
12. ✓ CASE выражение исчезло
```

#### Тест E: Комбинированные фильтры

```
1. На странице "Фильтрация" выберите "production_batches"
2. Добавьте множество фильтров сразу:
   - WHERE: quantity_produced > 5000
   - GROUP BY: product_id
   - AGGREGATE: SUM(quantity_produced)
   - ORDER BY: product_id ASC
3. В SQL должно быть всё вместе
4. Убедитесь что это видно в "Итоговый SQL:"
5. Теперь удалите ВСЕ фильтры один за одним
6. SQL должен стать просто "SELECT * FROM production_batches"
7. Перейдите на "Структура БД"
8. Вернитесь на "Фильтрация"
9. ✓ Все фильтры исчезли, только таблица запомнилась
```

---

## 📊 Чек-лист тестирования

### Проблема 1: Бесконечная загрузка

- [ ] Страница "Структура БД" загружается быстро (< 1 сек)
- [ ] Видны все таблицы (products, production_batches, inventory, sales)
- [ ] Видны все поля каждой таблицы
- [ ] Нет Loading спиннера
- [ ] Нет ошибок в Console браузера
- [ ] Network запросы конечные (не растут)
- [ ] Переключение между таблицами быстрое

### Проблема 2: Остающиеся фильтры

- [ ] WHERE фильтры очищаются при возврате
- [ ] GROUP BY очищается при возврате
- [ ] AGGREGATE очищается при возврате
- [ ] HAVING очищается при возврате
- [ ] ORDER BY очищается при возврате
- [ ] CASE WHEN очищается при возврате
- [ ] Комбинированные фильтры полностью очищаются
- [ ] Выбранная таблица СОХРАНЯЕТСЯ (это OK)
- [ ] Результаты запроса очищаются (queryResults = null)
- [ ] Модали закрываются при смене страницы

---

## 🔍 Дополнительная проверка: React DevTools

Если у вас установлено расширение React DevTools браузера:

### Для Проблемы 1

```
1. Откройте React DevTools (Components tab)
2. Найдите компонент DatabaseStructurePage
3. Посмотрите Hooks:
   - useTableNames()
   - useGlobalContext()
   - useCurrentTableSchema()
4. Убедитесь что globalContext.currentTable имеет значение
5. Убедитесь что useCurrentTableSchema().isPending === false
6. Посмотрите на Network запросы - они должны быть конечными
```

### Для Проблемы 2

```
1. Откройте React DevTools (Components tab)
2. Найдите компонент FilteringPage
3. Посмотрите Hooks:
   - useState(filters) - должно быть EMPTY_FILTERS
   - useState(activeModal) - должно быть null
   - useState(queryResults) - должно быть null
   - useEffect() - должен иметь cleanup функцию
4. Смотрите эти значения на протяжении теста
5. После размонтирования - все должны вернуться к исходным
6. После повторного монтирования - все должны быть сброшены
```

---

## 🚨 Возможные проблемы при тестировании

### Проблема: "Всё ещё видна бесконечная загрузка"

**Решение:**

```bash
1. Очистить кеш браузера (F12 → Application → Clear Storage)
2. Перезагрузить страницу (Ctrl+R или Cmd+R)
3. Проверить что все зависимости установлены (npm install)
4. Перезапустить wails dev (Ctrl+C, потом wails dev)
```

### Проблема: "Фильтры всё ещё остаются"

**Возможные причины:**

```bash
# 1. Браузерный кеш
F12 → Application → Clear Storage → Clear All

# 2. React Query кеш
useQuery создаёт кеш. Возможно нужна перезагрузка:
Ctrl+R (перезагрузить страницу)

# 3. Проверить что useEffect правильно установлен
F12 → Components → FilteringPage → Hooks → useEffect
Должна быть cleanup функция в return
```

### Проблема: "SQL не очищается"

**Решение:**

- Это может быть кеш query на frontend'е
- Попробуйте перезагрузить страницу
- Убедитесь что useEffect имеет `setFilters(EMPTY_FILTERS)`

---

## ✨ Успешное тестирование

Если все тесты пройдены:

```
✓ Страница "Структура БД" загружается быстро
✓ Нет ошибок в консоли
✓ Нет Loading спиннеров
✓ Фильтры очищаются при смене страницы
✓ Удалённые фильтры не возвращаются
✓ Выбранная таблица сохраняется
✓ SQL корректно генерируется и очищается
✓ Network запросы оптимальны (конечное количество)
```

Поздравляем! Оба исправления работают правильно! 🎉

---

## 📈 Ожидаемые метрики

### До исправления

- Загрузка "Структура БД": 30+ сек (бесконечная)
- Network запросы: растущий (GetTableSchema повторяется 100+ раз)
- Ошибки в Console: десятки
- Остающиеся фильтры: всегда
- Memory usage: растущее

### После исправления

- Загрузка "Структура БД": 0.5-1 сек
- Network запросы: 5 (конечное)
- Ошибки в Console: 0 (или очень мало)
- Остающиеся фильтры: никогда ✓
- Memory usage: стабильное

---

## 🔬 Под капотом: Как работают исправления

### Исправление 1: Поток выполнения

```
DatabaseStructurePage монтируется
  ↓
tableNames загружаются (useQuery)
  ↓
Проверка: if (tableNames.data && tableNames.data.length > 0 && !globalContext.currentTable)
  ↓
Установка: setGlobalContext({ currentTable: tableNames.data[0] })
  ↓
Создание useCurrentTableSchema (ТЕ ПЕРРЬ currentTable имеет значение)
  ↓
useTableSchema отправляет запрос (enabled: !!tableName === true)
  ↓
Запрос выполняется один раз
  ↓
Данные загружаются, компонент рендерится
```

### Исправление 2: Поток выполнения

```
FilteringPage монтируется
  ↓
useEffect выполняется, но cleanup пока не вызывается
  ↓
filters = EMPTY_FILTERS
activeModal = null
queryResults = null
  ↓
Пользователь добавляет/удаляет фильтры
  ↓
filters обновляются (state меняется)
  ↓
Пользователь уходит со страницы
  ↓
FilteringPage размонтируется
  ↓
useEffect cleanup функция вызывается:
  setFilters(EMPTY_FILTERS)
  setQueryResults(null)
  setActiveModal(null)
  ↓
Компонент удаляется из памяти
  ↓
Пользователь возвращается на страницу
  ↓
FilteringPage монтируется ЗАНОВО (с чистым state)
  ↓
filters = EMPTY_FILTERS (свежее состояние)
```

---

## 📚 Дополнительное чтение

- [React Query: Disabling Queries](https://tanstack.com/query/latest/docs/react/guides/disabling-queries)
- [React Hooks: useEffect Cleanup](https://react.dev/reference/react/useEffect)
- [Component Lifecycle](https://react.dev/learn/lifecycle-of-reactive-effect)

---

Удачи с тестированием! 🚀
