# Тестирование исправления: Загрузка данных в таблицы

## Шаг за шагом

### 1. Скомпилируйте приложение

```bash
cd /Applications/vscode/golang/DbMireaGolang

# Проверьте что Go код компилируется (опционально)
go build -o /tmp/test_build

# Или соберите полное приложение
wails build
```

### 2. Запустите development сервер

```bash
# В корневой папке DbMireaGolang
wails dev

# Приложение откроется на http://localhost:34115
```

### 3. На главной странице нажмите кнопку

```
[Пересоздать таблицы]
```

### 4. Вы должны увидеть одно из двух уведомлений:

#### ✅ Успешное исправление

```
✓ Таблицы успешно пересозданы и заполнены данными
```

#### ❌ Если есть ошибка

```
✗ Таблицы пересозданы, но не удалось загрузить тестовые данные
(Детальное сообщение об ошибке)
```

### 5. Проверьте что данные загрузились

#### Вариант A: Через UI приложения

1. Перейдите на страницу "Фильтрация"
2. Выберите таблицу **products** в dropdown'е
3. Нажмите кнопку [Выполнить] в GeneratedSQL
4. Вы должны увидеть 12 продуктов в таблице результатов:

```
┌────┬──────────────────────────┬────────────┬───────────┬───────┬────────────────┬────────────────────────────┐
│ #  │ product_id               │ name       │ flavor    │ ... │ caffeine_level │ ingredients                │
├────┼──────────────────────────┼────────────┼───────────┼─────┼────────────────┼────────────────────────────┤
│ 1  │ 1                        │ Энергетик  │ Лимон-мята│ ... │ high           │ вода, сахар, кофеин, ...  │
│ 2  │ 2                        │ Энергетик  │ Клубника  │ ... │ high           │ вода, сахар, кофеин, ...  │
│ 3  │ 3                        │ Энергетик  │ Ананас    │ ... │ high           │ вода, сахар, кофеин, ...  │
... (всего 12 записей)
```

#### Вариант B: Через psql (прямое подключение к БД)

```bash
# Подключитесь к PostgreSQL
psql -h localhost -U postgres -d energy_drinks_db

# Затем выполните:
SELECT COUNT(*) as product_count FROM products;
-- Результат: 12

SELECT COUNT(*) as batch_count FROM production_batches;
-- Результат: 20

SELECT COUNT(*) as inventory_count FROM inventory;
-- Результат: 12

SELECT COUNT(*) as sales_count FROM sales;
-- Результат: 25
```

#### Вариант C: Через логи приложения

Посмотрите в логи (logs/app.log) и найдите строки вроде:

```
[INFO] SEED: Начало загрузки тестовых данных
[INFO] SEED: Файл найден: /path/to/seed.data.sql
[INFO] SEED: Начало транзакции для загрузки данных
[INFO] SEED: Обновление последовательностей (sequences)
[INFO] SEED: Выполняем: SELECT setval('products_product_id_seq', ...)
[INFO] SEED: Транзакция успешно завершена
[INFO] === SEED: Загрузка тестовых данных завершена успешно ===
```

## Что должно произойти в логах

При нажатии кнопки "Пересоздать таблицы" вы должны увидеть в консоли:

```
[INFO] !!! НАЧАЛО ПЕРЕСОЗДАНИЯ ТАБЛИЦ В БАЗЕ ДАННЫХ !!!
[INFO] Попытка удаления таблицы: sales
[INFO] Таблица sales успешно удалена
[INFO] Попытка удаления таблицы: inventory
[INFO] Таблица inventory успешно удалена
[INFO] Попытка удаления таблицы: production_batches
[INFO] Таблица production_batches успешно удалена
[INFO] Попытка удаления таблицы: products
[INFO] Таблица products успешно удалена
[INFO] Удаление старого типа caffeine_level
[INFO] Создание нового типа caffeine_level
[INFO] Тип caffeine_level успешно создан
[INFO] Начало автоматической миграции таблиц
[INFO] Таблицы успешно созданы на основе моделей
[INFO] === ПЕРЕСОЗДАНИЕ ТАБЛИЦ ЗАВЕРШЕНО УСПЕШНО ===

[INFO] === SEED: Начало загрузки тестовых данных ===
[INFO] SEED: Поиск и чтение тестовых данных из файла: seed.data.sql
[INFO] SEED: Файл найден: /Applications/vscode/golang/DbMireaGolang/internal/database/seed.data.sql
[INFO] SEED: Начало транзакции для загрузки данных
[INFO] SEED: Обновление последовательностей (sequences)
[INFO] SEED: Выполняем: SELECT setval('products_product_id_seq', (SELECT COALESCE(MAX(product_id), 0) + 1 FROM products))
[INFO] SEED: Выполняем: SELECT setval('production_batches_batch_id_seq', ...)
[INFO] SEED: Выполняем: SELECT setval('inventory_inventory_id_seq', ...)
[INFO] SEED: Выполняем: SELECT setval('sales_sale_id_seq', ...)
[INFO] SEED: Транзакция успешно завершена
[INFO] === SEED: Загрузка тестовых данных завершена успешно ===
```

## Проверка каждой таблицы

### products таблица (12 записей)

```bash
# SQL запрос
SELECT COUNT(*) FROM products;

# Результат: 12

# Все продукты:
SELECT product_id, name, flavor, volume_ml, price, caffeine_level FROM products;
```

**Ожидаемые данные:**

- 3x Энергетик Boost (Лимон-мята, Клубника, Ананас)
- 3x Энергетик MaxCharge (Чёрная вишня, Голубика, Манго)
- 3x Изотоник Hydrate (Апельсин, Лимон, Арбуз)
- 3x Витаминный напиток VitaZest (Яблоко-морковь, Апельсин-имбирь, Гранат)

### production_batches таблица (20 записей)

```bash
SELECT COUNT(*) FROM production_batches;
# Результат: 20
```

**Ожидаемые данные:**

- Партии от 2024-02-01, 2024-02-02, 2024-02-03, 2024-02-10, 2024-02-15
- Разные объёмы производства в зависимости от типа напитка

### inventory таблица (12 записей)

```bash
SELECT COUNT(*) FROM inventory;
# Результат: 12

# Проверить статус склада:
SELECT product_id, quantity_in_stock, last_updated FROM inventory;
```

**Ожидаемые данные:**

- Остатки для каждого из 12 продуктов
- Даты обновления: 2024-02-29

### sales таблица (25 записей)

```bash
SELECT COUNT(*) FROM sales;
# Результат: 25

# Проверить продажи:
SELECT sale_id, product_id, sale_date, quantity_sold, total_price FROM sales
ORDER BY sale_date;
```

**Ожидаемые данные:**

- 25 транзакций продаж
- Даты продаж от 2024-02-05 до 2024-02-28
- Общая сумма продаж: 2,571,695.00

## Возможные проблемы и решения

### Проблема 1: "Файл seed.data.sql не найден"

**Решение:**

- Убедитесь что файл `seed.data.sql` находится в папке `internal/database/`
- Проверьте что путь корректен в консоли ошибки
- Попробуйте перезагрузить приложение

```bash
# Проверить файл
ls -la internal/database/seed.data.sql
# Должен вывести размер файла (~3KB)
```

### Проблема 2: "Таблицы пересозданы, но не удалось загрузить данные"

**Решение:**

- Проверьте логи на ошибку SQL
- Убедитесь что у пользователя PostgreSQL есть права на INSERT
- Проверьте синтаксис SQL в `seed.data.sql`

```bash
# Проверить логи
tail -f logs/app.log | grep -i "error\|seed"
```

### Проблема 3: "UNIQUE constraint violation"

**Решение:**

- Это значит что данные уже там есть
- Нажмите кнопку ещё раз (она удалит старые и создаст новые)
- Или вручную очистите таблицы

```bash
# Удалить все данные (осторожно!)
DELETE FROM sales;
DELETE FROM inventory;
DELETE FROM production_batches;
DELETE FROM products;
```

### Проблема 4: "Таблицы создались но данные не видны в UI"

**Решение:**

- Обновите страницу в браузере (F5 или Ctrl+R)
- Перезагрузите приложение
- Проверьте что правильно выбрали таблицу в dropdown'е

## Дополнительные проверки

### Проверка структуры таблиц

```bash
# В psql:
\d products
\d production_batches
\d inventory
\d sales
```

### Проверка что всё в порядке

```bash
# Проверить что все таблицы существуют
SELECT table_name FROM information_schema.tables
WHERE table_schema = 'public';

# Проверить что данные не повреждены
SELECT
  (SELECT COUNT(*) FROM products) as products,
  (SELECT COUNT(*) FROM production_batches) as batches,
  (SELECT COUNT(*) FROM inventory) as inventory,
  (SELECT COUNT(*) FROM sales) as sales;
```

**Ожидаемый результат:**

```
products | batches | inventory | sales
---------|---------|-----------|-------
   12    |   20    |    12     |  25
```

### Проверка indexes и constraints

```bash
# Все индексы
SELECT indexname FROM pg_indexes
WHERE schemaname = 'public';

# Все constraints
SELECT constraint_name, table_name
FROM information_schema.table_constraints
WHERE table_schema = 'public';
```

## Успешная проверка

Если вы выполнили все шаги выше и:

- ✅ Уведомление говорит "Таблицы успешно пересозданы и заполнены данными"
- ✅ В UI видны все записи из таблиц
- ✅ Логи показывают успешное завершение SEED операции
- ✅ psql запросы возвращают правильное количество записей

То исправление работает правильно! 🎉

## Откат (если что-то пошло не так)

Если нужно откатить изменения:

```bash
# Отката в git
git diff app.go  # Посмотрите что изменилось
git checkout app.go  # Откатить

# Или вручную:
# В app.go, метод RecreateTables() - удалите вызов database.SeedData()
```

## Заключение

Исправление простое но важное:

- **До**: Таблицы создавались но были пустые
- **После**: Таблицы создаются и автоматически заполняются тестовыми данными

Теперь приложение полностью готово к использованию и тестированию! ✨
